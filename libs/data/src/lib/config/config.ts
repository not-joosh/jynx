// Frontend configuration (Browser environment)

// Backend configuration (Node.js environment) - only available when explicitly imported
// Note: This is only exported for backend builds, not frontend
export { backendConfig } from './config.backend';
